History and changelog of the plugin

dev 0.1a:

Changes:

Scanner:
- Compass can now used to scan for portals
- Output with different colors in relation player - portal


Fixes:
/

dev 0.1b:

Portal:
- adding an inventory to the portal
- 4 different blocks for 4 different actions
- Adding inventory for "deploy" and "mods" actions
- Adding action, when player clicks on "hack" or "recharge"

Fixes:
- Adding else if for what block is clicked
	if STAINED GLASS blue / green= full PortalMenu
	if STAINED GLASS white  = PortalMenu only Hack and Deploy
